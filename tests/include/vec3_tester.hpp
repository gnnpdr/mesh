//#pragma once
//#include "vec3.hpp"



/*#include "vec3.hpp"


    using Vec3::Vec3f;
    
    Vec3f a(1, 2, 3);
    Vec3f b(4, 5, 6);
    
    auto c = a + b;
    std::cout << "a + b = " << c << "\n";  // (5, 7, 9)
    
    float dot = a.dot(b);
    std::cout << "dot = " << dot << "\n";  // 32
    
    auto cross = a.cross(b);
    std::cout << "cross = " << cross << "\n";  // (-3, 6, -3)
    
    float len = a.length();
    std::cout << "length = " << len << "\n";  // ~3.741
    
    // Проверка equals
    Vec3f a2(1.000001f, 2.000001f, 3.000001f);
    std::cout << "a equals a2? " << a.equals(a2) << "\n";  // 1 (true)
    
    return 0;*/